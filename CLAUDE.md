# 背单词 app + 词表项目

**新会话第一件事：`python3 scripts/status.py`** —— 它从 wordlists/ 现算进度、
闸门阈值和下一步动作，不会像写死的文档那样过期。本文件只写不会变的规格与规则。

## 两份词表，规格完全不同

| | A | B |
|---|---|---|
| 使用者 | 英语初学者 | 雅思 6 分以上 |
| 排序 | **按词频，由简到难** | **牛津高阶第 10 版词典序** |
| 收词 | 高频词，目标 5000 | **全量收录，约 6 万词条** |
| 每条义项 | 3 个 | **收全该词在牛津高阶的所有义项** |
| 每条结构 | 例句 + 讲解 | **「核心：」总括讲解** + 每个义项造句讲解 |
| 分批依据 | 每批约 45 词 | **每批例句总数**（go/do 这类词一个就几十句） |
| 合并单位 | 3000 词一份 | 待定 |

**A 绝不能排字母序** —— 它靠词频排出难度梯度，排字母序会毁掉这个梯度。
`merge-wordlist.py` 只对 B 重排。

## B 的收词边界（用户裁定，别自作主张）

只收**真正的单词**。

- ✗ **词缀条目**：`a-` `anti-` `-able` `-ation` —— 构词知识写进真实词条的「构词」行更有用
- ✗ **全大写缩写**：AA、AIDS、AOB
- ✓ 连字符复合词：able-bodied、age-old
- ✓ 多词固定条目：ad hoc、all right、air force、according to
- ✓ 外来短语：à la carte、a cappella、a priori、a fortiori
- ? 小写带句点缩写 a.m. 暂留，未经裁定（p.m./e.g./i.e./etc. 同此）

## 排序：按词，不是按字母

空格和连字符是词边界，边界处「更短的先排」：

```
a → a cappella → a fortiori → à la carte → a posteriori → a priori → aardvark
ad → ad hoc → ad lib → adage
all → all right → all-round → allay
always → a.m. → amalgam
```

忽略空格的话 `a priori` 会被拼成 apriori 排进 ap- 段 —— 那是错的。
排序键在 `scripts/wordkey.py`，**四个**脚本共用（resplit-b、coverage、
status、merge-wordlist），别再各写一份。merge-wordlist.py 曾经自造过一把，
两个错都犯了：忽略空格按字母排，把 a priori 拼成 apriori 排进 ap- 段；
不折重音，把 à la carte、à la mode 甩到全表最后。**合并文件是导进 app 的那一份，
顺序错了学的人直接看得见** —— 合并完要抽查首尾和重音词的位置。

## B 词条怎么写（格式是硬规则，app 的解析器按这个认）

每条词条一个段落，段落之间空一行。行的类型只有五种：

```
abrasive                          ← 首行：词头，只能是字母/空格/连字符/句点
核心：字面指"有磨蚀性的"（砂纸、清洁剂），比喻指人说话"刺人、       ← 核心讲解块
不客气"。后者是贬义，形容态度生硬到让人不舒服。                    （B 必需，可多行）
① Use an abrasive cleaner.        ← 例句：①②③…，最多 80 个
= 用带磨砂的清洁剂。               ← 译文：必须紧跟例句，以 = 开头
② He has an abrasive manner.
= 他态度很冲。
这里带贬义。                       ← 讲解行：随便写，跟在它所属的义项后面
③ Her abrasive tone offended him.
= 她生硬的口气冒犯了他。
名词用法：                          ← 标签行：以「：」结尾且不超过 24 字
abrasive = 研磨料                  ← 等式行：「英文 = 中文」，计入等式密度
近义对照：
abrasive = 刺人（态度粗糙）         ← 近义对照也写成等式形式，不写成散文
blunt = 直率（不绕弯）
harsh = 严厉（评判重）
```

**硬规则**（违反会被 check-wordlist.py 拦下）：

1. 首行必须像单词 —— 允许字母、重音字母、数字、空格、连字符、句点、斜杠；
   不能以连字符开头。斜杠是为 and/or、a/c、c/o、n/a、9/11 这类词头放行的
2. 每个例句**必须**紧跟一行以 `=` 开头的中文译文
3. 编号必须从 ① 开始且连续（写完跑 `renumber.py` 自动修）
4. B 的每条**必须**有「核心：」块，且**至少 2 个义项**
5. 译文里引用英文词**必须加引号**：`「a」和「the」是冠词`，否则判为中英混杂
6. 英文例句里不能混汉字，全文不能混韩文假名西里尔（输入法手滑，肉眼看不出）

**软要求**（批均，低于基线整批被拦）：等式 ≥2.13、讲解行/义项 ≥1.50。
近义对照写成等式形式（`abate = 平息（正式）`）既好读又计入密度，
写成散文行会让密度虚低、每批都要事后补。

## 「核心：」块写什么（用户 2026-09-04 裁定）

核心块讲的是**这个词**，四类内容：来源与构词、本质是什么、本质怎么延伸出
其余义项、这个词本身怎么用（词性、可数与否、搭配、语域、跟哪个近义词分界）。
**不写跟词无关的现实常识** —— 那种话看着有料，其实既不帮着理解这个词，
也不帮着记住它。

写过并已删掉的反面例子：biodegradable 写「能降解不等于降解得快，多数可降解
塑料要在工业堆肥的高温里才分得掉」（环保常识）、biodiversity 写「种类越杂，
系统越经得起冲击」（生态学常识）、bioethanol 写「原料多是玉米甘蔗，所以跟
粮价挂钩」（产业常识）、biodiesel 写「柴油机基本不改就能烧」（工程常识）。

同一批里该留的是另一类：biodefence 的「手段跟公共卫生防疫一样，分别在假想
对手是人」（什么时候该用这个词）、biodiesel 的「B20 是掺两成」（怎么读这个
词的写法）、bioethics 的「形式上是复数，当单数用」（语法）、bioengineering
的「两条线……共同点是把活物当成工程对象来造」（本质怎么长出两个义项）。

**长度没有硬性要求，按这个词实际有多少要讲来定（用户 2026-09-04 交代）。**
单义的术语词两句讲完就该收；多义词要讲语义架构和分支怎么长出来，
写长是应该的 —— back 的核心块 10 行、ball 9 行、bird 8 行，都不算超标。
**别拿行数当尺子去砍。** 该砍的是掺进来的现实常识，不是长度本身；
反过来为了凑长度硬讲，等于把上面那条规则反着违反一遍。
（曾经把「已有词条中位数 2 行 / 67 字符」写成硬指标，是记错了因果。）

**一句话判据：讲那个「东西」的删，讲那个「词」的留。**（2026-09-04 第二次纠偏）
这条规则我已经漂回去过一次 —— 写 bu- 段时又在核心块里写起了物种分类、
诺贝尔奖、中世纪黑死病、原产地、州树。它们看着有料，但学的人拿它们
既用不出这个词，也记不住这个词。

同一批里的正反例，照着判：

| 删 | 留 |
|---|---|
| buckwheat 不属禾本科、跟大黄是亲戚、无麸质、做荞麦面 | 名字里带 wheat 其实不是麦（构词误导） |
| buckminsterfullerene 的发现者拿了诺贝尔奖 | 名字来自建筑师 Buckminster Fuller（词源） |
| bubo 就是中世纪黑死病 | 腺鼠疫 bubonic plague 的病名从它来（构词） |
| brucellosis 靠生奶传播、巴氏消毒后少见 | 别名「波状热」得名于反复发热（构词） |
| bronze medal 得主比银牌得主心态好（心理学研究） | 跟金牌银牌成对说（搭配） |
| breakdancing 二零二四年进奥运 | 正式名称是 breaking 不是 breakdancing（用词） |
| body odour 是细菌分解汗液才有味 | 缩写 BO；英美两种拼法（用法） |
| boll weevil 十九世纪末毁掉美国南方棉业 | boll weevil 这个词由它构成（构词） |

**词源和词史算「来源」，可以写** —— bootylicious 二零零一年因一首歌进词典、
brontosaurus 的学名作废又恢复，讲的都是这个词本身的经历，不是那样东西的百科。

**单义的术语词不必每个义项都配一行散文讲解。** 「每个义项都要有讲解行」那条
是给多义常用词定的（action 那种，每一支都得说清怎么从本义长出来）；
asbestos、antibody 这类单义词的 ①② 就是「例句 + 译文 + 等式」，
等式行本身担的就是释义标签的活儿，再硬配一句只会稀释。

**复习模式只显示词头和英文例句** —— 核心讲解、译文、讲解行都不会漏出来，
所以中文内容可以放心写足。

## 批次大小按义项密度定，不按条数定（用户裁定）

一批写几条，取决于这批词有多少义项要覆盖，不是取决于凑够多少个词。
**多义词多的批次就少写几条** —— B 的规格是收全该词在牛津高阶的全部义项，
每个义项都要有例句和讲解，写不到就是没写全。

判断方法：动笔前先看这批里有几个是 go / air / alter 那种多义词。
一个 8 义项的词顶得上三四个单义的术语词。宁可一批只出 8 条，
也不要为了凑数把多义词压成 3 个义项。

**单义的术语词（植物名、化学名、医学名）3 个义项是够的**，
那不是写薄；**把多义的常用词压到 3 个义项才是写薄**。

**一次写的条数也有上限，跟密度无关（用户 2026-09-03 交代）。**
一口气写二三十条，后面几条的讲解会明显变水 —— 核心块开始套话、
义项开始靠复合词凑、讲解行退化成只有一行等式。**每批控制在 10 条上下**，
写完就跑闸门和 audit-depth，过了再写下一批。宁可多跑几轮，
不要为了少几次往返把质量摊薄。

## 核心词单独立项写，质量要远高于普通词条（用户裁定）

back、go、set、take、run、hand、line 这类高频多义词，是学的人反复
回来看的那几十个词。它们的词条质量决定整份词表值不值得用。

**核心词可以独占一批** —— 一批就写它一个，不必凑数。

具体要求，比普通词条高一档：
- 核心块要把**语义架构**讲出来：一个底层意思如何长出所有义项，
  哪几支是主干、哪几支是引申。不是罗列，是给出生成规则。
- **每个义项都要有讲解行**，说清这一支为什么这么来、
  搭配是什么、什么场合用。只给例句加译文不够。
- 短语动词与复合词成族出现时，要点出它们跟本义的关系，
  让人能推而不是背。
- 易混处必须单列辨析：back up 三义并存、back off 与 back down
  的分别、backward 与 backwards 的地域差异，这类地方最见功夫。

判断标准很简单：普通词条讲清「是什么」，核心词条还要讲清
「为什么是这些意思」和「什么时候用哪个」。

**义项编号到 80**（①-㊿ 再接 ❶-❿ ⓫-⓴ ⓵-⓾ 三组反白圈码）。
原来只有 50 个，那是脚本里写死的字符表带来的偶然限制，不是设计裁定 ——
set / go / take 在牛津高阶里义项都远超 50，卡在 50 会逼人删义项。
已同步改到 80：`scripts/renumber.py`、`scripts/check-wordlist.py`
的 NUMS 与允许字符集、`index.html` 的解析与渲染，三处必须一起改。
超过 80 时 renumber.py 会明确报错而不是抛 IndexError。

**什么留在核心词里，什么拿出去单列** —— 这是内容判断，跟编号够不够无关：
能从底层义推出来的（短语动词、习语、比喻用法）留在核心词里，
学的人看得到它们怎么从一个意思长出来；只是碰巧同词根的具体名词
（backplane、backstitch、backlot）拿出去单独立条，它们多半是单义的，
塞进核心词只会把那张卡片撑成一堵墙。

## 语域标注：有地域或年代限制的词必须标出来

全量收录意味着会遇到大量**有使用限制**的词。收是要收的，但学的人必须知道
什么时候不能用。牛津高阶给这些词都有标签，词条里要对应写出「注意语域」行：

| 类型 | 例子 | 标注要写什么 |
|---|---|---|
| 地区变体英语 | `ach`（苏格兰/爱尔兰）、`arty`（英式） | 哪个地区用，别处不说 |
| 国别变体借词 | `achcha` `aapa` `achkan`（印度英语） | 标 Indian English，不是印地语词 |
| 古旧词 | 只保留「词还在用、某个义项古旧」的 | 注明哪个义项旧了、现在该说什么 |
| 专业术语 | `abscissa` `acrolect` `ablative` | 哪个学科用 |
| 冒犯性词 | `airhead` `Abo` `asylum`（作精神病院讲） | 明确写「不礼貌」「已过时」 |
| 商标通名化 | `Android` `AstroTurf` | 注明原为商标 |

**不收的两类**（名单在 `reference/exclude.txt`，coverage.py 自动剔除）：
① **地区变体英语与国别变体借词** —— ach（苏格兰）、achcha / aapa / achkan（印度英语）、
   arvo / billabong / bludger（澳新）、blether（苏格兰与北英）、biltong / boerewors（南非）。
   **2026-09-04 用户补裁的通则：除英式与美式之外的一切国别与地区变体，词头属哪一种都剔。**
   已点名的有苏格兰、北英、爱尔兰、澳新、印度、南非、东非；没点名的照同一条办，不必再请示。
   只剔词头本身属该变体的 —— 通用词的某个义项属该变体（billy、bide、barrack）、
   澳新的动植物名（bandicoot、bilby、barramundi）都留着，判据见 exclude.txt 表头。
② **窄专业生僻词与整词已废的古旧词** —— acrolect、accidence、abscissa、actinium、
accelerando、acanthus、aconite、ague、asunder、anon、abaft。
**不要用「学习者会不会遇到」当判断线** —— 那是主观的，一用就开始自作主张地筛，
a 段第一次只收 10%、ab- 段只收 31% 都是这么来的。排除只按客观类别走，
其余一律收，不管看起来多冷僻。拿不准就收 —— 少收的代价（学的人搜不到）更大。

**注意只删「整词已废」的古旧词** —— abide、ail、alms、aforesaid、asylum
都是词还在用、只是某个义项旧了，这些保留，在词条里注明就好。

**别把这些混称「方言」** —— 中文的「方言」指同一语言下的地方话，
而 `achcha` 是印度英语的词、`ach` 是苏格兰英语的词，两者都是英语的
**地区变体**，和 British/American English 是同一层级的区分。

## 多词条目归哪一条：看重心，不看第一个词也不看虚词表（用户裁定）

规则②说「多词条目并进词根」，但「哪个是词根」要按**重心**判，
不能机械地取内容词或第一个词：

- **构词成分不算内容词**。agro-industry 的重心在「农业」，
  agro- 永远不会单独立条，等 industry 到 i 段也等不来正确归属 ——
  它跟 agribusiness、agrochemical、agroforestry 同族，就在 ag- 段解决。
  同理 all-star 的重心在 all，alpha male 的重心在 alpha。
- **`all` 与 `any` 不是虚词**，已从 coverage 的 FUNCTION_WORDS 里拿掉。
  它们跟 at / by / in 不是一回事：段里 anybody / anytime / all right /
  all-round 都各自立条，两个词的写法没道理被推去 s 段或 m 段。
- **前缀相同不等于同源**。coverage 按前缀猜宿主，经常猜错：
  afara 不是 afar 的派生（西非木材树名）、agogo 不是 agog、
  akinesia 不是 akin、albumen 不是 album、allotrope 不是 allot、
  altocumulus 的 alto- 是「中层云」不是女低音、analgesic 不是 anal、
  angstrom 不是 angst、annular 不是 annul、Antiguan 不是 anti-。
  这类一律另立词条，并在词条里写一行「注意别混」。

## 复合词是并入还是单列：按常用度分流（用户裁定）

一个词根带出几十个多词条目时（air 有 41 个、all 有 37 个、
back 有 90 余个），不要一刀切。**高频的各自立条，冷僻的并进词根**：

- 立条：air traffic control、all-time、backache、baseball —— 学的人
  真会遇到，值得有自己的卡片。
- 并入：air commodore 那一族军衔、all-points bulletin、
  back button —— 冷僻或成族，做成对照块反而更清楚。

判断标准是「学的人会不会在一般阅读里碰到」，不是词频表。
段里已有的先例是最好的依据：ai- 段已有 air force / air raid /
air conditioning 独立成条，后来的 air X 就照这个走。

## 并入失效的两种模式（写完必须跑 coverage 验收）

`derived_covered` 认「派生词以词根前四个字母打头」，
所以下面两种并入写了也不算数，必须改成单列或另立词头：

1. **词根不足 3 个字母**：aptness 并进 apt 不算（下限已从 4 放宽到 3，
   1-2 个字母的词根仍然不认）。
2. **派生词换了字母**：baking powder 并进 bake 不算 ——
   「baking」不以「bake」打头。解法是先立 baking 这个词头，
   那一族才认得上。

所以每批写完都要跑 coverage，不能凭「我写进去了」就算数。

## 清单的清洗伪影：已处理的与必须逐条判的

`reference/` 那份词头清单是机器清洗过的，留下几类伪影。
coverage.py 里已经自动处理的（都逐条核对过零误伤）：

| 伪影 | 数量 | 处理 |
|---|---|---|
| 撇号洗成连字符（adam-s-apple、ain-t、macy-s） | 198 | 还原成撇号 |
| 商标粘死（airbustm、coketm、bluetoothtm） | 128 | 一律过滤 |
| 同形词编号（bass2、lead2、used1） | 47 | 全小写且裸形存在才去数字 |
| 单字母连字符缩略语（a-m = a.m.、e-g = e.g.） | 15 | 拼合 |
| 括号标可选部分（AS (level)、(the) Netherlands） | 9 | 去括号 |
| 推不出来的撇号（baha-i、maitre-d） | 2 | 显式对照表 |

**不能自动处理、必须逐条判的两类**：
- **裸词与词缀条目并存** 130 处。多数裸词是真词（able、age、all、
  anti、cross、ex、Afro），自动删会毁掉一批常用词；
  只有 aholic、ance、arian、ary 这种确实是词缀掉了连字符。
- **所有格专名** 106 条。清洗把 Aesop's Fables 洗成小写的
  aesop-s-fables，绕过了「首字母大写才查 keep 名单」那一步，
  于是 hadrian-s-wall、king-s-cross、mother-s-day 这类纯专名
  会一路报缺。到各字母段逐条判，判完记进 exclude.txt。

## 并入怎么做

派生词和多词条目不单列，写进词根条里并**补例句**。做法是在目标词条末尾追加：

```
⑤ She answered abrasively.        ← 新义项，编号接着原有的往下排
= 她答得很冲。
abrasively = 生硬刺人地            ← 等式行说明这个派生形式
```

改完跑 `renumber.py` 重排编号，再 `resplit-b.py` 重新分块。
`coverage.py` 会去词根条的正文里搜这个词，搜到就算覆盖。

## 工作流

```bash
python3 scripts/status.py                    # 看状态
python3 scripts/pull-pending.py <词...>       # 从归档区取回复用（只读）
python3 scripts/renumber.py '<glob>'          # 重排 ①②③ 编号
python3 scripts/check-wordlist.py 'wordlists/A-*.txt' 'wordlists/B-*.txt'
python3 scripts/resplit-b.py                  # 按词典序重排并均匀分块
python3 scripts/audit-prefix.py a             # 查有没有整段漏掉
python3 scripts/audit-depth.py a              # 查多义常用词有没有被写薄
python3 scripts/pull-pending.py --prune       # 清理已收录的归档条目
python3 scripts/merge-wordlist.py A 3000      # 合并成可导入的大文件
```

**闸门从三件套变成四件套（2026-09-05 起）。**
每批写完依次跑 check-wordlist → coverage → audit-depth → **audit-swallowed**。
最后这个查「被吞掉的词」：清单里的词只在宿主的核心块、构词行或注意行里
被顺嘴提了一句，coverage 的 derived_covered 就判它已收，于是它永远不会
出现在待新写里 —— carriage 被 car 吞、camphor 被 camp 吞、comfort 被
comfit 的「注意别混」吞，同一个坑栽了三次才写脚本。
补救二选一：在宿主里给它补一条真正的义项（例句 + 译文 + 等式），
或者把宿主那行里的字面词换成中文描述，让它回到待新写里单独立条。
往已有词条尾部补义项用 scripts/add-sense.py，补完跑一遍 renumber.py。

**提交不跟着每一轮输出走（用户 2026-09-04 交代）。** 一次输出可以写好几批，
闸门三件套（check-wordlist → coverage → audit-depth）每批都要跑，
但不必每批都提交、也不必每轮结束都问一句要不要提交 ——
攒到一个自然的节点（一个双字母段收完、或用户开口）再提交。

**开新字母段之前必须先跑 `pull-pending.py`。** 归档区放着早期超前写的词，
到相应字母段该取回复用。开 b 字母时忘了跑，结果 baby / back / bad / bag /
balance / ball / ban / band / bank 等 11 条从头重写了一遍。
（那次损失只是白费力气 —— 归档版都薄得多，新版是升级；
但流程该走还得走。）正确做法：取回当核对清单 → 按当前标准重写 →
写完 `--prune`。

**动笔前必须先列出该字母段的牛津高阶词条清单跑差集。** 凭手感列词已经出过四次
漏收，而且每次都是用户发现的：

| 何时 | 漏了什么 |
|---|---|
| a 段初次 | 只收了 10%（autumn、adjacent、acquaintance 这类常用词都不在） |
| ab- 段 | 只收了 31% |
| ao- / aa- | 整段跳过 |
| al- 段 | albeit、although（归档区一直摆着，我没核对） |
| as- 段 | 宣布收全 77 条，实际应有 110 条，漏 32 条 |

**「宣布收全」不等于收全** —— 我列的清单本身就是凭记忆写的，它短，
差集自然显示「无遗漏」。**差集只能证明「我列的都收了」，不能证明「我列全了」。**
抽查证实这是系统性的：ao- 漏 aoudad，aq- 漏 aquaplane/aquavit，
at- 漏 atavistic/atheism/atoll/atonement/atrium/attaché/attainable/
attenuate/attribution/attune。

**词头清单已经到位**：`reference/OALD10_Cleaned_Learning_List.csv`，67264 条。
完整性检查从此是机器的事：

```bash
python3 scripts/coverage.py a        # 按双字母段报缺词
python3 scripts/coverage.py ab ac    # 看具体缺哪些词
```

脚本按用户裁定的边界过滤清单（词缀、全大写缩写不收），并把清单里同一条目的
空格版与连字符版（abide by / abide-by）归一化合并。

两条用户裁定已写进脚本：
① **首字母大写的条目只收「已经进了词汇」的** —— Achilles tendon、Adam's apple、
Ayurvedic medicine、Alzheimer's 收，纯人名地名商标（Aaron、Aberdeen、Aga™）不收。
名单在 `reference/proper-nouns-keep.txt`，
逐条过完记在那里，不再重复讨论。
② **多词条目和派生词一律并进词根的词条，不单列** —— abide by、abound in 写进
abide / abound 条，abrasively、abrasiveness 写进 abrasive 条，并各配例句。
只有词根本身也没收时才新立词条（这时要把词根反向补进来）。
**并进哪条看内容词，不是看第一个词** —— in advance 并进 advance、by accident
并进 accident，并进 in / by 毫无意义；反过来 abound in、abide by 的内容词在前。
脚本有一份虚词表专门处理这件事。内容词还没轮到（at large 的 large 在 l 段）就
推迟，内容词根本不是英语词条（ab initio 的 initio）则按独立外来短语新写。
缺口报告因此分三栏：待并入 / 待新写 / 待推迟。

**第一次跑它就推翻了此前的结论**：我宣布「a 字母全部收全 1288 条」，
实际清单有 3021 条，只收了 41%，26 个双字母段无一收全。漏的主要是三类：
多词条目（absolute zero、absentee landlord、above board）、
短语动词（abide by、abound in）、派生形式（abrasively、absent-mindedness）
—— 全是我此前当作「写进别的词条里的注释」而没单列的。
**每段动笔前和收尾后都必须跑 coverage.py，不要再凭记忆列清单。**

## 写薄了闸门量不出来：用 audit-depth.py 逐条查

`check-wordlist.py` 量的是**批均**密度，一批里多义词写薄、术语词写厚，
均值照样过闸。`scripts/audit-depth.py` 逐条量，并拿 A 词表的词频序当
常用度标尺 —— A 越靠前越高频，高频词只有 3 个义项就是可疑的。

两类报告，处理方式不同（原有的「核心块过短」已撤，见上节：核心块长度不设标准）：

| 报告 | 含义 | 怎么办 |
|---|---|---|
| 压薄嫌疑 | 高频词只有 ≤3 个义项 | 逐条对牛津高阶补义项；确属单义的放过 |
| 讲解偏少 | 讲解行不足义项数的 1.5 倍 | 给缺的义项补讲解行 |

**「讲解偏少」是最要紧的一类。** 等式行虽然计入讲解行，但它只是个词义
标签；一个义项光有「例句 + 译文 + 等式」，等于告诉学的人"这一支是这个
意思"，没告诉他"这一支为什么会有这个意思"。a 段初查 68 条不达标，
典型的是 action：17 个义项、16 行讲解，①②⑤⑥⑦ 全是例句加译文。
**多义词的每个义项都要有一行说它怎么从本义长出来** —— 这是用户裁定的
质量线，也是这份词表值不值得用的分水岭。

## 编辑内规不能写进词条正文

词条是给学的人看的，收词边界、清单口径这些只跟编者有关的话不能漏进去。
写过「全大写缩写不单立条」「那是变体词，本表不收」「同名的排行榜是专名，
本表不单立条」—— 学的人看到这些只会莫名其妙，闸门也查不出来。
要说地域差别就直说那个词是哪里用的，不要说本表收不收它。

## 词表是纯文本，不是 Markdown

`index.html` 的解析器只认这五种行，不解析 Markdown。**写 `**加粗**`
会原样显示成星号**，闸门也查不出来（`*` 在允许字符集里）。
a、b 两段清理时一次揪出 76 行。写词条时不要用任何 Markdown 记号，
要强调就靠断句和「」引号。

## 重写已有词条会悄悄删掉并入的词

补讲解时整条重写，很容易把原来并入在里面的派生词或异体拼法丢掉，
`check-wordlist.py` 查不出来（格式没问题）。重写 `all` 时就把
「也写作 all-singing-all-dancing」删了，a 段当场从 100% 掉到 99%。

**改完已有词条，跑 coverage.py 复查那个字母段** —— 不只是新写时才跑。

## 质量闸门：改尺子之前先想清楚

闸门在 `check-wordlist.py`，pre-commit 钩子会拦住不合格的提交。

**已经废弃过五把尺子**，规律很清楚：**凡是「除以词条数」的批均指标，都会被
「这批碰巧是哪些词」左右，量不出投入**。字符/词条、字符/义项、批均义项都因此
作废。现在留下的是逐条规则（每条至少 2 个义项、必须有「核心：」块）加两个不随
多义性漂移的密度指标。

**闸门拦下时，先判断是我写薄了还是尺子错了，别条件反射去调基线。** 曾为消一个
误报放宽中英混杂检查，结果放过了真错误（「他kicked了球」）。正确做法是改内容：
引用英文词加引号、近义对照写成 `abate = 平息（正式）` 的等式形式。

## 已经踩过的坑

- **`git add -A` 盲提交**曾把用户在 Finder 里的删除一并提交，线上 404。提交前
  先 `git status --short` 看一眼。
- **`chunks[-2] += chunks.pop()`** 的求值顺序陷阱覆盖掉一整块，丢了 25 条词条，
  靠 checker 的跨批查重才发现。
- **pull-pending 曾「取出即删除」**，取出的内容被临时文件覆盖后 17 条词就没了。
  现已改为只读 + `--prune`（依据从「打算收」变成「确实收了」）。
- **拼接归档条目要显式补空行**，否则前后两块会粘成一条（aegis 曾被 aesthetic 吞掉）。
- **小尾巴文件会拉低批均**，`resplit-b.py` 已改为均匀分块，别再切「满 25 留尾巴」。
- **回填等式行按行号插入时，先改的插入会让后改的行号失效**——同一个词多处插入
  必须按位置从大到小倒着插，不能顺着从前往后插。第一版 `backfill-eq.py` 这么写，
  `bad` 词条里第 8 个义项的等式插进了第 7 个义项的地界，肉眼查出来才发现。
- **等式行插在「近义对照/注意别混/注意区别/反义词」标签块之后等于白插**——
  coverage 判定标签块内的内容属于「讲别的词」，插在后面仍会被判定成缺失。
  必须插在这类标签块**之前**。`backfill-eq.py` 已经处理好这条，别再手写插入脚本
  重踩一次。
- **同一词条内两个不同义项的等式右侧文字完全相同，L1 出题会给出两个一样的选项**
  ——写等式行时要扫一眼这个词其余义项的等式，撞了就换个说法，不要图省事复制
  近义表述。`scripts/backfill-eq.py` 跑完后配合覆盖率脚本里的撞车检测一起查。

## app 本体

源码 `index.html` 单文件 PWA，数据存 IndexedDB(`vocab_v1`)，设置存 localStorage。
已部署到 GitHub Pages：https://teamtimteamtimteam-sys.github.io/vocab-trainer/
开发服务器见 `~/.claude/launch.json` 的 `vocab-trainer`（python3 http.server 8413）。
参数集中在 `index.html` 顶部常量 `GROUP_TARGET / LADDER / LADDER_TIER /
MULTISENSE_THRESHOLD / MULTISENSE_STAGE_CAP / DAY_START`。

### 复习：词级调度 + 四档出题（2026-09-03 grilling 后整体重写，替换了旧的
「组级调度、只显示例句自评」设计，旧设计的原因已不成立，不要按旧版文档改回去）

**为什么整组重写**：旧版复习把中英释义藏起来，让人看着例句自己判断「有没有
印象」——测的是 familiarity（眼熟），不是 retrieval（想不想得起来），
易词难词耗一样的时间，而且间隔从 `completedAt` 算错了方向。新版解决三件事：
判分客观化、难度分层、间隔算对起点。

**调度下沉到词，不是组**：每个词自己的 `w.reviewStage`（0-7）和
`w.nextReviewDate` 独立走。组只剩「背诵时一批学的词」这一层意义——不再持有
`reviewStage`/`nextReviewDate`/`reviewSession`，`state` 只剩
`pending/learning/reviewing` 三态，没有「已掌握」，因为掌握与否现在是逐词判断。

**一条统一阶梯，同时管间隔和题型**（`LADDER`/`LADDER_TIER`），不是两个独立
旋钮——这是唯一改过一次方向的设计点：最初想让间隔和题型正交（各自一套状态），
写实现时发现正交讲不通「怎么从 L1 升到 L2」，改成一条阶梯更干净，也顺带把
「长期轮换」和「毕业」统一成同一件事：单义词爬到 stage 7（90天）就是进了长期
轮换，不需要另开一个「毕业池」——多义词（`MULTISENSE_STAGE_CAP` 定的，
≥`MULTISENSE_THRESHOLD` 个义项）永远卡在 stage 5（30天/L3），不许再往上，
所以他们会反复出现，不会被判定「学会了」。

答对：`w.reviewStage` 前进一级（多义词撞到 cap 不再涨）。
答错：`w.reviewStage` 后退两级，下限 0——这一步顺带保证「当天不再考」：
退到 0 时 `LADDER[0]=1` 天，最快也是明天，不用另记「今天问过」这件事。

**四档题型，全部从词条格式自动生成，不需要人工出题**——这是能撑到 6 万词的
唯一原因：
- **L1 认支线**：给例句，从这个词自己的其他义项的等式行抽干扰项
- **L2 辨邻居**：优先用「近义对照」块里点名的邻居词，缺了退回 L1 的取法
- **L3 反推核心**：给 1-2 句例句猜核心块，干扰项是别的词的核心块（自动遮住
  词头本身，34% 的核心块里写着词头，直接摆出来会泄题）；干扰项优先级：
  对照块邻居 → 前缀相同的形态邻居 → 本次复习会话里的其他词。这一档任何词条
  都出得了题（核心块 100% 覆盖），是最后一道兜底
- **L4 反向产出**：给中文释义 + 首字母，打出词头本身（不是整条搭配短语）

数据缺口自动降级：某个义项没等式行就跳过它、只从别的义项抽；实在不够就退到
L3；L3 也生成不了（核心块也空）才退到最原始的自评（「记不清/还记得」）——
这个兜底几乎不会被触发，只在词条数据本身有洞时才出现。

**等式行（`词条格式`那节里的「英文 = 中文」那一行）现在是硬需求**——它既是
给人看的词义标签，也是机器判分的答案键。写词条时顺手带上，不要删掉已有的
散文讲解去换等式行，两者分工不同，都要有（2026-09-03 用户交代）。

**背诵批次按义项数贴边界，不按词数**（`packBatches()`，跟
`scripts/resplit-b.py` 切文件是同一个思路：不是攒够就切，是逐词比较「现在切」
和「带上它再切」哪个更接近目标，选更接近的，外加一条下限防止批太碎）。
理由：一个词的信息量能差 14 倍（`back` 50 义项 ≈ 14 个普通 3 义项词），
按词数切组会让有的批次比别的批次重出一倍还多。目标 230 义项/批（`GROUP_TARGET`，
可调），对应约 58-64 词/天的节奏，是「3 年内背完全书」倒推出来的，不是精确值，
跑几批感觉不对可以直接改这一个数字。

**门禁三条不变**：今日到期复习没答完锁背诵；已有一组在背锁开新组；一天只能
开一组新组。变的只是「到期复习」的判定单位——从「组里的复习跑完」变成
「今天到期的词都答过」（答对答错都算「答过」，不缩水）。

**「记不清」当场展开全文，但当天不再考这个词**——这是故意的，用户原话「
不刷，答错立刻展开全文，但当天不再考，明天第一批出现」。会看到复习正确率
比自评时代明显下降，那是真实水平浮出水面，不是退步。

**长期轮换的词还会回来**：单义词爬满阶梯（stage 7/90天）后不是「再也不用管」，
每 90 天还会被抽到复习队列里考一次（L4），答错就按正常规则退两级重新爬。
多义词因为卡在 stage 5，本来就在正常轮回里，不需要单独的「长期池」机制。

### 视觉

基底近白 `#FBFBFA`，绿与墨黑只渗在四角，中心用 `--core` 洗回近白保证正文不带色。
玻璃两级：内容面板 `--glass` .86、chrome `--glass-chrome` .72。品牌绿 `#1B8A3D`
用于色块/填充/进度环；浅色底上的**小号绿字**另用 `--green-text` `#177C34`
（品牌绿配白字只有 4.42，差 AA 一点点）；深色下两者都是 `#40B968`。
**绿色只用于重点与掌握状态，不作装饰**。字体三个角色：英文衬线 / 中文苹方 /
**数据等宽**（组号、12/50、R3、日期）—— 等宽绝不套在中文标签上，否则字距会把
「这里：」拉开。签名元素是螺旋进度环 `ring()`。用户明确否决过背景里的大螺旋球
线稿，不要再加回来。

收藏夹：星标在词卡标题栏，存 meta 的 `starredIds` 数组（数组顺序即收藏先后，
不动 IndexedDB schema）。复习点「记不清」给词的 `forgotCount` +1，供「忘得最多」
排序。**星标用墨黑不用绿** —— 绿在本项目表示「重点与已掌握」，收藏夹装的是没掌握
的词，用绿会让语义打架。

### app 相关的坑

① 主题差异**一律走 CSS 变量**，绝不写 `html[data-theme=dark] .某组件` —— 它的
优先级 (0,2,1) 会压过 `.btn.ghost`(0,2,0) 这类变体，曾导致深色下四个按钮黑字黑底。
② 别用 `JSON.stringify(字符串)` 往 `onclick="..."` 里拼参数，双引号会截断属性；
用 data 属性 + 事件委托。
③ JS 里的内联 `style="color:..."` 会压过 CSS 规则，改设计系统时要一起清。
④ 用户量级目标 10 万+ 词、分批导入，避免任何全表 `getAll()`（备份导出已改成
游标分块）。
⑤ iPad 上必须从主屏幕图标进入才能豁免 Safari 的 7 天存储清理。
**iOS Safari 打不开本地 file:// 网页**，必须走 HTTPS 托管 —— 这也是 Service
Worker 的硬性前提（局域网 http:// 不是安全上下文，注册会失败）。
⑥ **origin 绑定存储**：换域名等于清空进度，只能靠导出/导入备份 JSON 迁移。
⑦ 改代码后 `git push` 即自动重新部署；SW 用 stale-while-revalidate，
用户下次打开生效。
⑧ **本地开发服务器也会被 SW 缓存**——改完 `index.html` 直接刷新看不到效果，
因为 SW 拦截请求返回的还是旧脚本，改了却像没改一样，容易误判成代码没生效。
调试时先 `navigator.serviceWorker.getRegistrations()` 挨个 `unregister()`、
`caches.keys()` 挨个 `delete()`，再刷新。这次调试复习流程时真的中过一次招：
明明已经把 `reviewAdvance()` 的 bug 改掉了，浏览器里复现的还是旧行为。
